// Write a program that:

// Creates an array called fruits
// Adds "Mango" using push()
// Removes the last fruit using pop()
// Prints the final array
// Prints the number of fruits using .length

let fruits =["Apple","Orange","Banana"];
fruits.push("Mango");
console.log(fruits)
fruits.pop();
console.log(fruits)
console.log(fruits.length)