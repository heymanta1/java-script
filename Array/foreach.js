 // forEach() is used to run some code for every item in an array.

 let students = ["Ram", "Shyam", "Hari", "Sita"];

students.forEach(function(student) {
    console.log(student);
});


// practice questions

let numbers = [10, 20, 30, 40];

// Q3. Use forEach() to print every number.
numbers.forEach(function(numbers){
    console.log(numbers);
});